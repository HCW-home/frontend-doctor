import { TranslatedGenderPipe } from './translated-gender.pipe';

describe('TranslatedGenderPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslatedGenderPipe();
    expect(pipe).toBeTruthy();
  });
});
