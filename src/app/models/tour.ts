export enum Direction {
    Forwards = 0,
    Backwards = 1
}

export enum EventName {
    StepHide = 'stepHide',
    StepShow = 'stepShow'
}

export enum TourType {
    INVITES_MENU = 'invites-menu',
    NEW_INVITE_BUTTON = 'new-invite-button',
    REMOTE_PATIENT_INVITE = 'remote-patient-invite',
    INVITE_FORM_CONTACT_INPUT = 'invite-format-contact-form',
    INVITE_FORM_SEND_LINK_MANUALLY_INPUT = 'invite-format-send-link-manually-input',
    WAITING_ROOM_MENU = 'waiting-room-menu',
    OPENED_CONSULTATIONS_MENU = 'opened-consultations-menu',
    CONSULTATION_HISTORY_MENU = 'consultation-history-menu',
    HEADER_PROFILE_MENU = 'header-profile-menu',
}
